services:
  mailhub_web_backend:
    build:
      context: ./backend
    environment:
      - API_PREFIX=/api
      - ENV=production
    expose:
      - "8000"
    volumes:
      - ./STATE.md:/app/STATE.md:ro
    networks:
      - web

  mailhub_web_frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE: /api
    expose:
      - "80"
    networks:
      - web
    labels:
      caddy: "${CADDY_SCHEME-http://}mailhub.${HOST_SUFFIX:-localhost}"
      caddy.0_reverse_proxy: "/api* mailhub_web_backend:8000"
      caddy.1_reverse_proxy: "/mcp* mailhub_web_backend:8000"
      caddy.2_reverse_proxy: "/state-doc* mailhub_web_backend:8000"
      caddy.3_reverse_proxy: "mailhub_web_frontend:80"
